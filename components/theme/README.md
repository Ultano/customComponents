# theme-toggle

Simple component to switch between light and dark themes by toggling the `dark-theme` class on the document body.

## Usage
```html
<script type="module" src="../../components/theme/theme-toggle.js"></script>
<link rel="stylesheet" href="../../styles/theme.css" />
<theme-toggle></theme-toggle>
```

Customize the button via CSS if needed.
