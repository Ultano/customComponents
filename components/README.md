# Components

Each folder contains an isolated Web Component. Include them individually in your page using ES modules.

Example:
```html
<script type="module" src="components/card/custom-card.js"></script>
```

The components rely on CSS variables defined in `styles/theme.css`.
